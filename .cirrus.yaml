env:
  TASKS_TO_RUN: "windo"
only_if: $TASKS_TO_RUN == '' || $CIRRUS_TASK_NAME =~ $TASKS_TO_RUN

linux_task:
  container:
    image: ubuntu:latest
  script:
    - echo hello

windows_task:
  windows_container:
    image: cirrusci/windowsservercore:2019
  script:
    - echo hello
